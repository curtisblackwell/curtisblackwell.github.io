{
  "scripts": {
    "watch": "npx tailwindcss -i ./src/input.css -o ./site.css --watch",
    "minify": "npx tailwindcss -o site.css --minify"
  },
  "dependencies": {
    "tailwindcss": "^3.1.8"
  }
}
