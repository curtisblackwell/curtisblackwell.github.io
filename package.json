{
  "private": true,
  "scripts": {
    "build": "node-sass --output-style=compressed cb.scss cb.css",
    "watch": "node-sass --output-style=compressed --include-path=node_modules --source-map=cb.css.map --watch cb.scss cb.css | browser-sync start --config=bs-config.js"
  },
  "devDependencies": {
    "browser-sync": "^2.18.8",
    "node-sass": "^4.5.2",
    "system-font-css": "^2.0.1"
  }
}
